CXX=g++
RM=rm -f
LIBS = -framework GLUT -framework OpenGL
CXXFLAGS=-Wno-deprecated
BINDIR=../../bin

SRCS=ex8.cpp
OBJS=$(subst .cpp,.o,$(SRCS))
BIN = ex8

all: $(BIN)

$(BIN): $(OBJS)
	$(CXX)  -o $(BIN) $(OBJS) $(EXTS) $(LIBS) $(CXXFLAGS)

install: $(BIN)
	mv $(BIN) $(BINDIR)

clean:
	$(RM) $(OBJS);\
	$(RM) $(BIN);

distclean: clean
	$(RM) $(BINDIR)/$(BIN)



